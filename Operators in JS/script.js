'use strict';

// let answer = +prompt('Есть ли вам 18?', 'Да');

// console.log(typeof(answer));

// console.log('arr' + ' — object');
// console.log(4 + ' — object');

let incr = 10, // инкремент — операция увеличивающая переменную на 1.
    decr = 10; // дикремент — операция уменьшающая переменную на 1.

incr++; // = 10 + 1
decr++; // = 10 - 1

console.log(5 % 2); // = 1 --> 5 / 2 = 2 + (остаток 1)

console.log("2" == 2); // true
console.log("2" === 2); // false

let isChecked = true,
    isClosed = false;

console.log(isChecked || isClosed); // && — И, || — ИЛИ
